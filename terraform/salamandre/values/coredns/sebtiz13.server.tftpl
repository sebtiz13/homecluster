${domain}:53 {
  errors
  hosts {
    %{ for name in subdomains ~}
    ${server_ip} ${name}.${domain}
    %{ endfor ~}
    fallthrough
  }
  kubernetes cluster.local 10.0.0.0/24
  forward . /etc/resolv.conf
  cache 3600
}
