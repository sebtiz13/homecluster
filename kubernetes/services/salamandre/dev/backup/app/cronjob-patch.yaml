apiVersion: batch/v1
kind: CronJob
metadata:
  name: zfs-s3-backup
spec:
  suspend: true
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: backup-runner
            volumeMounts:
            - name: certificate
              mountPath: /etc/ssl/certs/vm-ca.crt
              subPath: tls.crt
              readOnly: true

          volumes:
          - name: certificate
            secret:
              secretName: vm-ca-tls
              defaultMode: 420
