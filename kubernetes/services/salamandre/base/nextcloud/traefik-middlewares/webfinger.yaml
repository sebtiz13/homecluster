apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: webfinger-nodeinfo
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
spec:
  redirectRegex:
    regex: https?://([^/]*)(/.well-known[^#]*)
    replacement: https://${1}/index.php${2}
    permanent: true
