domain_name: 'baku.{{ root_domain }}'

k3s:
  flags:
    - --tls-san='{{ domain_name }}'

# TODO: Until ArgoCD release https://github.com/argoproj/argo-cd/issues/7799
labeled_namespaces:
  - name: minio
    tags:
      - minio
  - name: adguard
    tags:
      - adguard
