- ansible.builtin.import_playbook: setup.yaml
- ansible.builtin.import_playbook: k3s_requirements.yaml
- ansible.builtin.import_playbook: k3s_system.yaml
- ansible.builtin.import_playbook: k3s_infrastructure.yaml
- ansible.builtin.import_playbook: k3s_cloud.yaml

- name: Configure backup
  hosts: salamandre
  become: true
  tags:
    - backup
    - minio
  roles:
    - role: backup
      when: "(ansible_limit is not defined) or ('baku' in ansible_limit)"
