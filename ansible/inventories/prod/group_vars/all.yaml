env: production
git_branch: main

# The pool type (available values: stripe, mirror, raidz (similar to RAID5) or raidz2 (Similar to RAID5 with dual parity))
zfs_pool_type: raidz

# Webhook url for send discord notifications
alerts_discord_webhook_url: "" # TODO: secret it
