host_cidr: "{{ ansible_eth0.ipv4.address }}/{{ ansible_eth0.ipv4.netmask }}"

basic_auth_users:
  - name: admin
    password: admin

smtp_auth:
  host: mailhog.mailhog.svc
  port: "1025"
  username: "contact@{{ root_domain }}"
  password: password
