argument_specs:
  main:
    short_description: The main entry point for the database access.
    options:
      host:
        type: str
        default: postgresql.loc
      database:
        type: str
        required: true
      username:
        type: str
        required: true
      password:
        type: str
        required: true
      secret_path:
        type: str
        required: true
