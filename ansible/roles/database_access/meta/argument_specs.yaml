argument_specs:
  main:
    short_description: The main entry point for the database access.
    options:
      secret_path:
        type: str
        required: true
      database:
        type: str
        required: true
      username:
        type: str
        required: true
