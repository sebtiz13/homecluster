argument_specs:
  main:
    short_description: The main entry point for the ArgoCD cluster role.
    options:
      cluster_name:
        type: str
        required: true
      cluster_url:
        type: str
        required: true
