argument_specs:
  main:
    short_description: The main entry point for the database access.
    options:
      namespace:
        type: str
        required: true
      middleware_name:
        type: str
        required: true
      secret_name:
        type: str
        required: true
      users:
        description: List of users
        type: list
        required: true
