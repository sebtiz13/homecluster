argument_specs:
  main:
    short_description: The main entry point for the ArgoCD Vault role.
    options:
      manifest:
        type: str
        required: true
      secrets:
        type: list
      restart_resources:
        type: list
        default: []
