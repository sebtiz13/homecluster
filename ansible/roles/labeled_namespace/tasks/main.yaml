- name: Create namespace
  kubernetes.core.k8s:
    kubeconfig: '{{ kubeconfig.dest }}'
    state: present
    resource_definition:
      apiVersion: v1
      kind: Namespace
      metadata:
        name: '{{ item }}'
        labels:
          domain: '{{ domain }}'
  loop: '{{ labeled_namespaces }}'
