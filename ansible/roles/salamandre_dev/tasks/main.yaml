- name: Deploy Mailhog application manifest
  tags: mailhog
  kubernetes.core.k8s:
    kubeconfig: "{{ hostvars.salamandre.kubeconfig.dest }}"
    state: present
    src: "{{ manifests_folder }}/mailhog.yaml"
    namespace: "{{ hostvars.salamandre.apps_argocd.namespace }}"
