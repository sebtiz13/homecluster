validate_certs: true

# The base path for secrets
secrets_base_path: ''

# List of secret want create
secrets: []
  # - path: app/auth
  #   data:
  #     username: admin
  #     password: "{{ lookup('ansible.builtin.password', '/dev/null') }}"
  # - full_path: app/data/auth
  #   data:
  #     username: admin
  #     password: "{{ lookup('ansible.builtin.password', '/dev/null') }}"
