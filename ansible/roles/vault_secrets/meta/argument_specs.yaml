argument_specs:
  main:
    short_description: The main entry point for the Vault secrets role.
    options:
      secrets_base_path:
        description: 'The base path for secrets'
        type: str
      secrets:
        description: List of secret want create
        type: list
      validate_certs:
        type: bool
