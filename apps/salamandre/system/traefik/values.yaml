plugin: helm

source:
  repoURL: https://traefik.github.io/charts
  chart: traefik
  targetRevision: 26.0.0

destination:
  namespace: traefik

syncPolicy:
  options:
    createNamespace: true
  automated:
    prune: true
    selfHeal: true
