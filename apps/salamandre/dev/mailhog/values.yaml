plugin: helm

ingress:
  subdomain: mail

source:
  repoURL: https://codecentric.github.io/helm-charts
  chart: mailhog
  targetRevision: 5.2.3

destination:
  namespace: mailhog

syncPolicy:
  createLabeledNamespace: true
  automated:
    prune: true
    selfHeal: true
