server:
  volumeMounts:
    - name: certificate
      mountPath: /etc/ssl/certs/ca.crt
      subPath: tls.crt
  volumes:
    - name: certificate
      secret:
        secretName: vm-ca-tls-secret
        defaultMode: 420
        optional: true
