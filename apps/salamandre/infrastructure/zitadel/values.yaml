plugin: argocd-vault-plugin-helm

ingress:
  subdomain: sso

source:
  repoURL: https://gitlab.com/api/v4/projects/40446088/packages/helm/devel
  chart: zitadel
  targetRevision: 0.0.1 # 7.3.2

destination:
  namespace: zitadel

syncPolicy:
  createLabeledNamespace: true
  automated:
    prune: true
    selfHeal: true
