plugin: argocd-vault-plugin-helm

source:
  repoURL: https://charts.gitlab.io/
  chart: gitlab-agent
  targetRevision: 1.22.0

destination:
  namespace: gitlab

syncPolicy:
  automated:
    prune: true
    selfHeal: true
