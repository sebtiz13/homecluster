config:
  kasAddress: wss://kas.{{ .Values.baseDomain }}
  token: <path:salamandre/data/gitlab/agents/local#token>

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi
