plugin: helm

ingress:
  subdomain: vault-secrets

source:
  repoURL: https://helm.releases.hashicorp.com
  chart: vault
  targetRevision: 0.22.0

destination:
  namespace: vault

syncPolicy:
  automated:
    prune: true
    selfHeal: true
