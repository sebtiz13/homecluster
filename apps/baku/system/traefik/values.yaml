plugin: helm

source:
  repoURL: https://helm.traefik.io/traefik
  chart: traefik
  targetRevision: 17.0.5

destination:
  namespace: traefik

syncPolicy:
  options:
    createNamespace: true
  automated:
    prune: true
    selfHeal: true
