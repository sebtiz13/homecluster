plugin: helm

source:
  repoURL: https://openebs.github.io/zfs-localpv
  chart: zfs-localpv
  targetRevision: 2.2.0

destination:
  namespace: kube-system

syncPolicy:
  automated:
    prune: true
    selfHeal: true
