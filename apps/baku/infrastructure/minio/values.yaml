plugin: argocd-vault-plugin-helm

source:
  repoURL: https://charts.min.io/
  chart: minio
  targetRevision: 4.0.15

destination:
  namespace: minio

syncPolicy:
  automated:
    prune: true
    selfHeal: true
