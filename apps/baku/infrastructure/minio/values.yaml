plugin: argocd-vault-plugin-helm

ingress:
  subdomain: baku

source:
  repoURL: https://charts.min.io/
  chart: minio
  targetRevision: 5.0.1

destination:
  namespace: minio

syncPolicy:
  automated:
    prune: true
    selfHeal: true
