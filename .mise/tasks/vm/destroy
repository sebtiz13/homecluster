#!/usr/bin/env bash
#MISE description="Destroy virtual machines"
#MISE depends=["init"]
#MISE interactive=true
#USAGE flag "--host <host>" help="Limit to only specified machine" {
#USAGE   choices "salamandre" "baku" "all"
#USAGE   default "all"
#USAGE }

set -euo pipefail
# shellcheck disable=SC1091
source "$(dirname "$0")/../_lib.sh"

run_ansible vms_destroy dev "--ask-become-pass $(limit_flag "${usage_host:-}")"
