nameOverride: null

project: project
destination:
  server: salamandre
  namespace: default
source:
  repoURL: ""
  chart: ""
  targetRevision: ""
syncPolicy:
  automated:
    {}
    # prune: true
    # selfHeal: true
  # https://argo-cd.readthedocs.io/en/stable/user-guide/sync-options
  options:
    {}
    # applyOutOfSyncOnly: true
    # # Need valid value ( foreground, background or orphan )
    # prunePropagationPolicy: foreground
    # pruneLast: true
    # replace: true
    # failOnSharedResource: true
    # respectIgnoreDifferences: true
    # createNamespace: true

plugin: "helm"
appValues: ""
appValuesEnv: null
# Used for plugin "argocd-vault-plugin-helm"
helmArgs: ""
# Used for plugin "helm"
valueFiles: []
