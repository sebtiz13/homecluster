# Default values for _external-secrets.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nameOverride: ""
fullnameOverride: ""

secretStoreRef:
  name: vault-eso
  kind: ClusterSecretStore

refreshInterval: 1h

secrets:
  []
  # Create an external secret (doc: https://external-secrets.io/latest/api/externalsecret/#example)
  # - name: secret-name # The name of resource (prefixed by fullname)
  #   target: {}
  #   dataFrom: {}
  #   annotations: {}
