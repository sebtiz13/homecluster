apiVersion: v1
kind: Endpoints
metadata:
  name: {{ include "adguard-proxy.fullname" . }}-endpoint
subsets:
  - addresses:
      - ip: {{ .Values.adguard.ip }}
    ports:
      - port: {{ .Values.adguard.port }}
        name: {{ include "adguard-proxy.fullname" . }}
        protocol: TCP
