apiVersion: v1
kind: Service
metadata:
  name: {{ include "adguard-proxy.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: {{ include "adguard-proxy.fullname" . }}
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.adguard.port }}
